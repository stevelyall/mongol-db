selector:
  js_test:
    roots:
    - jstests/fail_point/*.js

# Failpoint tests start their own mongold's.
executor:
  js_test:
    config:
      shell_options:
        nodb: ''
        readMode: commands
