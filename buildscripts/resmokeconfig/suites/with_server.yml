executor:
  js_test:
    config:
      shell_options:
        readMode: commands
    fixture:
      class: MongoDFixture
      mongold_options:
        set_parameters:
          enableTestCommands: 1
